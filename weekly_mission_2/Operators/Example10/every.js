const names = ['Luis Felipe', 'Eduardo', 'Miguel', 'Alejandro']
const areAllStr = names.every((name) => typeof name === 'string') //every
console.log("Ejemplo 10: Son todos los nombres string:" + areAllStr)